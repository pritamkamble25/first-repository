import { LightningElement } from 'lwc';
import myImageResource from '@salesforce/resourceUrl/user_image'
export default class StaticImage extends LightningElement {
    userImage = myImageResource
    // localvariabel
}